{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Schéma de validation pour une fiche de révision de séquence",
  "description": "Définit la structure attendue pour une fiche de questions de révision pour une séquence pédagogique.",
  "type": "object",
  "properties": {
    "titre": {
      "type": "string",
      "description": "Titre de la fiche de révision."
    },
    "description": {
      "type": "string",
      "description": "Description brève du contenu de la fiche de révision."
    },
    "questions": {
      "type": "array",
      "description": "Liste des questions de révision (10-20 questions).",
      "items": {
        "type": "object",
        "properties": {
          "question": {
            "type": "string",
            "description": "Texte de la question."
          },
          "ressource": {
            "type": "string",
            "description": "Nom de la ressource où trouver la réponse."
          },
          "seance": {
            "type": "string",
            "description": "Nom de la séance associée à cette question."
          },
          "reponse": {
            "type": "string",
            "description": "Réponse attendue à la question."
          },
          "niveau_difficulte": {
            "type": "string",
            "enum": ["facile", "moyen", "difficile"],
            "description": "Niveau de difficulté de la question."
          }
        },
        "required": ["question", "ressource", "reponse"]
      }
    }
  },
  "required": ["titre", "questions"]
}
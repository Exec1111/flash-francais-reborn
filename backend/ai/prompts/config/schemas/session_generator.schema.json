{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "sessions": {
      "type": "array",
      "description": "Liste des séances générées",
      "items": {
        "type": "object",
        "properties": {
          "title": {
            "type": "string",
            "description": "Titre descriptif de la séance"
          },
          "date": {
            "type": "string",
            "format": "date-time",
            "description": "Date de la séance (ISO 8601)"
          },
          "notes": {
            "type": "string",
            "description": "Notes détaillant le contenu et l'organisation de la séance"
          },
          "duration": {
            "type": "integer",
            "description": "Durée estimée en minutes"
          },
          "sequence_id": {
            "type": "integer",
            "description": "ID de la séquence parente"
          },
          "objective_ids": {
            "type": "array",
            "description": "Liste des IDs des objectifs pédagogiques associés",
            "items": {
              "type": "integer"
            }
          },
          "oeuvre_ids": {
            "type": "array",
            "description": "Liste des IDs des œuvres à exploiter dans la séance",
            "items": {
              "type": "integer"
            }
          },
          "notes": {
            "type": "string",
            "description": "Description détaillée du contenu, des activités et de l'organisation de la séance"
          },
          "duration": {
            "type": "integer",
            "description": "Durée estimée en minutes (optionnel)"
          }
        },
        "required": ["title", "date", "sequence_id", "notes", "oeuvre_ids"]
      },
      "minItems": 1,
      "maxItems": 20
    }
  },
  "required": ["sessions"]
}
[pytest]
markers =
    timeout: "Définit un timeout par test (plugin pytest-timeout)"
# Timeout global (peut être surchargé par @pytest.mark.timeout)
timeout = 60
# Méthode thread recommandée pour compatibilité multi-OS
timeout_method = thread
